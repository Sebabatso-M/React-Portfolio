import styled from 'styled-components';

export const AboutMeInfoWrapper = styled.p`
    color: var(--m-grey);

    margin: 0 auto;
    margin-bottom: var(--mb-val-s);
    max-width: 62ch;

    .orange-highlight {
        color: var(--orange);
    }
`;
